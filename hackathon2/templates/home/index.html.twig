{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}
{% block header %}
    <style type="text/css">
        .jumb
        {
            height: 35% !important;
            background-image: url("https://cdn.hipwallpaper.com/i/67/99/VJlgdD.jpg");
            background-repeat: repeat-x;
            background-size: cover;
            width: 100% !important;
        }
    </style>
    <div class="container-fluid jumb">
        <div class="row">
            <div class="col-md-12 text-center text-white my-5">
                <h1 class="h1">Viens rencontrer des knacks chaude près de chez toi !!</h1>
            </div>
        </div>
        <div class="row my-3">
            <div class="col-md-3"></div>
            <div class="col-md-3 text-right mt-5 ml-3 p-2 boutjumb">
                <a href="" class="btn btn-warning font-weight-bold">Commander</a>
            </div>
            <div class="col-md-5 text-center imgjumb">
                <img src="https://image.noelshack.com/fichiers/2019/26/4/1561650226-cuisine.png" alt="...">
            </div>
        </div>
    </div>
{% endblock %}
{% block body %}
    <div id="carouselExampleControls" class="carousel slide mt-5" data-interval="false" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <div class="card-deck justify-content-center">
                    {% for toutresto in toutresto|slice(0,5) %}
                        <div class="mx-5">
                            <a href="{{ path('restaurant', { 'id': toutresto.id }) }}"><img class="card-img-top" src="{{ toutresto.logo }}" alt="Card image cap" style="height: 150px"></a>
                            <p class="text-white">{{ toutresto.name }}</p>
                        </div>
                    {% endfor %}
                </div>
            </div>
            <div class="carousel-item">
                <div class="card-deck justify-content-center">
                    {% for toutresto in toutresto|slice(6,5) %}
                        <div class="mx-5">
                            <a href="{{ path('restaurant', { 'id': toutresto.id }) }}"><img class="card-img-top" src="{{ toutresto.logo }}" alt="Card image cap" style="height: 150px"></a>
                            <p class="text-white">{{ toutresto.name }}</p>
                        </div>
                    {% endfor %}
                </div>
            </div>
            <div class="carousel-item">
                <div class="card-deck justify-content-center">
                    {% for toutresto in toutresto|slice(11,5) %}
                        <div class="mx-5">
                            <a href="{{ path('restaurant', { 'id': toutresto.id }) }}"><img class="card-img-top" src="{{ toutresto.logo }}" alt="Card image cap" style="height: 150px"></a>
                            <p class="text-white">{{ toutresto.name }}</p>
                        </div>
                    {% endfor %}
                </div>
            </div>
            <div class="carousel-item">
                <div class="card-deck justify-content-center">
                    {% for toutresto in toutresto|slice(16,5) %}
                        <div class="mx-5">
                            <a href="{{ path('restaurant', { 'id': toutresto.id }) }}"><img class="card-img-top" src="{{ toutresto.logo }}" alt="Card image cap" style="height: 150px"></a>
                            <p class="text-white">{{ toutresto.name }}</p>
                        </div>
                    {% endfor %}
                </div>
            </div>
            <div class="carousel-item">
                <div class="card-deck justify-content-center">
                    {% for toutresto in toutresto|slice(21,5) %}
                        <div class="mx-5">
                            <a href="{{ path('restaurant', { 'id': toutresto.id }) }}"><img class="card-img-top" src="{{ toutresto.logo }}" alt="Card image cap" style="height: 150px"></a>
                            <p class="text-white">{{ toutresto.name }}</p>
                        </div>
                    {% endfor %}
                </div>
            </div>
            <div class="carousel-item">
                <div class="card-deck justify-content-center">
                    {% for toutresto in toutresto|slice(26,5) %}
                        <div class="mx-5">
                            <a href="{{ path('restaurant', { 'id': toutresto.id }) }}"><img class="card-img-top" src="{{ toutresto.logo }}" alt="Card image cap" style="height: 150px"></a>
                            <p class="text-white">{{ toutresto.name }}</p>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
        <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
{% endblock %}